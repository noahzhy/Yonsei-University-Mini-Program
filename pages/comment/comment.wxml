<!--pages/comment/comment.wxml-->
<view class='content'>
  <view class='col'>
    <text class='title-max'>写新评价</text>

    <view class='divLine' style='margin-top:16rpx'></view>

    <text class='title-mini-gray'>给分</text>
    <view class='row-wrap'>
      <block wx:for="{{grade}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view>    

    <text class='title-mini-gray'>出席</text>
    <view class='row-wrap'>
      <block wx:for="{{attend}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view>  

    <text class='title-mini-gray'>课题</text>
    <view class='row-wrap'>
      <block wx:for="{{homework}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view> 

    <text class='title-mini-gray'>小组讨论</text>
    <view class='row-wrap'>
      <block wx:for="{{group}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view>

    <text class='title-mini-gray'>考试次数</text>
    <view class='row-wrap'>
      <block wx:for="{{exam}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view>

    <view class='divLine' style='margin-top:16rpx'></view>

    <text class='title-mini-gray'>课程总价</text>
    <view class='row-wrap'>
      <block wx:for="{{point}}"
        data-item="item">
        <text class="{{item.check?'text-check':'text-uncheck'}}">{{item.txt}}</text>
      </block>
    </view>

    <text class='title-mini-gray'>详细评价</text>
    
    <textarea class='textarea'
      maxlength="300"
      placeholder="填写更多相关信息"
      placeholder-style='text-align:top'>
    </textarea>
    
    <button class='button-add'
      bindtap='addNewFinish'>提交评价</button>
  </view>
</view>
